import React from 'react';
import {makeStyles} from "@material-ui/styles";
import {Box, Grid} from "@material-ui/core";

const useStyles = makeStyles({

});

export default () => {
    return (
        <Grid item xs={5} component={Box} display={{ xs: 'none', lg: 'flex' }}>
            
        </Grid>
    );
};
